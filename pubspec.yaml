name: flutter_fins_protocol
version: 1.0.0
description: "Flutter Fins Protocol library is a fins protocol based communication library for Omron plc."
homepage: https://reposis.com
repository: https://github.com/reposis/flutter_fins_protocol
issue_tracker: https://github.com/reposis/flutter_fins_protocol/issues

environment:
  sdk: '>=3.4.1 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  hive: ^2.2.3
  buffer: ^1.2.3
  process_run: ^1.2.4
  http: ^1.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  assets:
    - assets/script/